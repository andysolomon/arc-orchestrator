# Explore: Automatic Delegate Wrapper

The runner already contains runner-routing-v3 phase and workload stacks. Explicit public aliases intentionally resolve to one candidate; `composer-implement` maps to `composer-2.5`.

The Claude orchestration skill is limited to the Agent tool. Its available implementation wrapper invokes `--backend composer`, and no neutral Agent exists for an automatic run. Generated Pi, Cursor, and Copilot guidance includes the v3 policy but still foregrounds explicit backend examples.

